# RestDNS Bind9 client crontab file

# Uncomment the following line to enable restdns-bind:
#  (don't forget to customize the restdns server address)

#* * * * * bind restdns-bind --syslog http://my-restdns-server/ /etc/bind/restdns/